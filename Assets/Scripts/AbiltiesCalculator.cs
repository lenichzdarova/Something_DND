
static class AbiltiesCalculator
{
    public static Abilities GetAbilities(Character character)
    {        
        var abilities = character._abilities;

        //iterate through all effect, find IAbilitiesProvider and apply it    

        return abilities;
    }
}
